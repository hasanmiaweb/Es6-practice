function namess(x, y) {
  return x + y;
}

const myFun = (x, y) => {
  return x + y;
};

const total = myFun(4, 5);

console.log(total);
//
const handleFun = (namewss, toa) => {
  let names = "Hasan";
  let age = 45;
  const toal = names + " " + age;
  return toal;
};

console.log(handleFun());
